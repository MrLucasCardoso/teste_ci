machine:
  python:
    version: 3.5.0
  services:
    - docker

dependencies:
  override:
    - docker build -t teste_ci .

deployment:
  production:
    branch: master
    commands:
      - sudo deploy.sh
